# task（工匠）Agent 中途确认 → 卡住问题报告

> 问题发现时间：2026-07-27 | 报告版本：v1.0
> 目标：供 oc-plus 插件优化 task/subagent 交互机制

---

## 一、问题复现

### 操作序列

```
1. 主 agent 用 task 工具启动「工匠」agent
2. 指令：批量更新 12 个岗位 MD 文件，按规则 A/B/C/D 执行
3. 规则 A 和规则 D 有冲突（同一个文件、同一行要加不同内容）
4. 工匠在执行中发现了冲突，停下来确认
5. 工匠返回确认问题的同时，task 被标记为 COMPLETED
6. 主 agent 回复「规则 D 版本为准，继续执行」
7. 这条回复变成了普通对话消息，出现在用户面前
8. 工匠已经终止，不再接收任何新输入
```

### 关键时间线

```
m00052  task(工匠) 启动                          ← 状态：running
m00053  用户问「做完了吗？」                      ← 工匠已停但用户不知道
m00054  task_result = "completed"                ← 工匠只返回了确认问题就结束了
m00055  主 agent 回复「规则 D 版本为准」          ← 这条消息到了用户
```

---

## 二、根因分析

### 2.1 task 工具的行为模型

根据实际行为反推，task 工具的交互模型是**单轮请求-响应**：

```
task({
  prompt: "完整指令",       ← 启动时一次性传入
  subagent_type: "工匠"
})
         ↓
   agent 执行（独立上下文）
         ↓
   返回结果 → 标记 completed  ← 返回即终止，无法续接
```

**不存在「agent 中途提问 → 主 agent 回答 → agent 继续」的交互路径。**

### 2.2 为什么工匠会在中途确认

工匠收到的规则 A 和规则 D 要求对「后端开发.md」的票据专场表格追加不同内容：

| 规则 | 要求追加的行 |
|------|-------------|
| 规则 A | `银行银企直连接口 \| 描述银行接口文档 → AI 生成对接代码 + 对账逻辑` |
| 规则 D | `银行银企直连接口 \| 描述银行接口文档 → AI 生成对接代码 + 对账逻辑 + 异常重试` |

规则 D 比规则 A 多了 `+ 异常重试`。工匠判定这是冲突，需要澄清——这是合理的行为（agent 不应自作主张）。

### 2.3 但是

工匠不应该在「确认问题」这一步就终止。正确的行为应该是：

1. ~~返回确认问题~~ ← 这导致了 completed
2. **持续等待主 agent 的回复**
3. 收到回复后继续执行

---

## 三、影响评估

### 对当前会话的影响

| 影响 | 详情 |
|------|------|
| **任务未完成** | 12 个文件的批量更新只执行了工匠的确认步，后续修改由主 agent 自己完成 |
| **用户体验差** | 工匠的确认问题被 task_result 吞掉，用户看不到；主 agent 的回复却出现在用户面前，用户困惑 |
| **主 agent 误判** | 主 agent 以为工匠还在运行，回复后发现工匠已停，只能自己接手 |

### 对通用场景的影响

这个 bug 的核心矛盾是：

> **task 工具设计上是「一次性代理人」，但 agent 的 system prompt 被赋予了「遇到冲突要确认」的能力。**

两套机制互相矛盾：
- task 工具：发出去 → 等结果 → 结束
- agent 行为：不确定 → 提问 → 等答案 → 继续

当 agent 在 task 框架内提问时，task 框架无法处理——它只认「返回结果 = 完成」。

---

## 四、建议修复方向

### 方案 1：task 工具支持双向通信（理想方案）

在 task 工具层面增加「暂停等待」状态：

```
task 状态机：
  running → waiting_for_input → running → completed
                ↑
         agent 提问触发
                ↓
         主 agent 回复后恢复
```

**实现要点**：
- task_result 不再自动标记 completed，而是标记 paused
- 需要一个新的工具或参数让主 agent 向 paused 的 task 发送回复
- 超时机制：paused 超过 N 分钟自动取消

### 方案 2：禁止 agent 在 task 中提问（简单方案）

修改工匠的 system prompt，在 task 执行场景下禁止提问：

```
在 task 模式下执行时：
- 遇到规则冲突 → 按最后一个规则执行
- 遇到不确定的参数 → 使用默认值
- 遇到需要确认的信息 → 在返回结果中标注"未确认"
- 禁止停下来等待用户输入
```

**优点**：改动小，不改变 task 工具行为
**缺点**：agent 可能在不确定的情况下执行错误操作

### 方案 3：task 支持多轮回调（折中方案）

task 允许在 prompt 中预埋多个「确认点」和对应的默认答案：

```json
{
  "prompt": "...",
  "fallback_decisions": {
    "rule_conflict": "prefer_rule_D",
    "missing_info": "use_default"
  }
}
```

agent 遇到冲突时直接查 fallback_decisions，不提问。

---

## 五、短期 workaround（当前可用）

在给 task 工具写 prompt 时，主动预判冲突并写好优先级：

```
❌ 不好的提示词：
  批量更新 12 个文件，按规则 A/B/C/D 执行

✅ 好的提示词：
  批量更新 12 个文件，按规则 A/B/C/D 执行。
  规则优先级：D > A，遇到冲突按规则 D。
  不确定的参数取默认值。不需要停下来确认，直接执行。
```

这样 agent 不需要提问，就不会触发卡住问题。

---

## 六、相关消息引用

| 消息 ID | 内容 |
|---------|------|
| m00052 | task(工匠) 启动并返回确认问题 |
| m00053 | 用户问「做完了吗？」 |
| m00054 | 用户问「那句话怎么回复到我这里了」 |
| m00063–m00064 | 主 agent 解释根因 + 用户确认要报告 |

---

*建议优先级：方案 1（长期）> 方案 3（中期）> 方案 2（不推荐，agent 质量下降）。短期先用第五节 workaround。*

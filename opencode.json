{
  "$schema": "https://opencode.ai/config.json"

  // ── 模型别名（agent markdown 中引用，deploy.mjs 部署时替换）──
  // ⚠️ 别名不写在本文件：OC 严格校验 opencode.json，未知键（modelAliases）
  //    会抛 ConfigInvalidError 导致启动失败（2026-08-08 实际踩坑）。
  // 别名已独立到 model-aliases.json（纯构建期配置，deploy.mjs 读取，OC 不加载）。
  // DS_MODEL_LOW = 编码执行等轻量任务（Flash）
  // DS_MODEL_HIGH = 推理、审查等重度任务（Pro）
  // DS_MODEL_VISION = 制图师（Kimi K3，多模态前端视觉强，贵——按需调用）
  // 修改别名后重新部署生效：node deploy.mjs
}

# 分形 Guardian Agent — 机制说明

> 面向人类用户的架构文档。设计决策与原理见 [设计.md](设计.md)。
> 当前版本：V3.6 | 2026-07-23

---

## 一、一句话概述

分形是一个**后台 Guardian 插件**——静默观察你在 OpenCode 中的所有操作，在五个关键场景自动介入（提醒、纠正、提取知识），不打断你的工作流。

---

## 二、整体架构

```mermaid
flowchart TB
    subgraph OC["OpenCode 运行时"]
        SP["system prompt<br/>（每轮构建）"]
        CM["chat.message<br/>（用户消息到达）"]
        EV["event hook<br/>（所有系统事件）"]
    end

    subgraph Fractal["分形 Plugin"]
        ST["system.transform<br/>注入规则 + 知识索引 + 断言提醒"]
        WARN["chat.message<br/>同轮警告注入 + 习惯确认检测"]
        EVT["event handler<br/>事件日志 + 审查队列 + 断言检测 + session 结束处理"]
        ANALYZE["LLM 自主学习<br/>分析 events.log 发现新习惯"]
        COMMIT["提交知识提取<br/>轮询 git log 提取值得保留的知识"]
    end

    subgraph Storage["持久化存储"]
        BLOCKS["blocks/<br/>知识块（按 status 分目录）"]
        TRIGGERS["triggers/<br/>触发规则（glob 匹配）"]
        EVENTS["events.log<br/>事件原始日志"]
        COUNTER[".assertion-counter.json<br/>触发线4 分级计数"]
    end

    OC --> Fractal
    Fractal --> Storage
    ANALYZE --> BLOCKS
    ANALYZE --> TRIGGERS
    COMMIT --> BLOCKS
```

**三个 hook 入口，按执行顺序：**

| 顺序 | Hook | 做什么 |
|------|------|--------|
| 1 | `system.transform` | 构建 system prompt：注入核心规则 + 知识索引 + 触发线提醒 + 计划摘要 |
| 2 | `chat.message` | 用户消息到达：注入同轮警告（触发线2/4） + 检测"确认习惯"关键词 |
| 3 | `event` | 监听所有系统事件：记录日志 + 断言检测 + 审查队列 + session 结束处理 |

---

## 三、Hook 管道：一轮对话的完整时序

```mermaid
sequenceDiagram
    participant U as 用户
    participant OC as OpenCode
    participant ST as system.transform
    participant CM as chat.message
    participant EV as event hook
    participant LLM as 大模型

    U->>OC: 发送消息

    Note over OC,ST: 构建 system prompt
    OC->>ST: 触发
    ST->>ST: 注入核心规则（corerules.md）
    ST->>ST: 注入知识索引（每5轮）
    ST->>ST: 注入触发线4 断言提醒（按等级）
    ST->>ST: 注入触发线5 知识提取（检查新提交）
    ST->>ST: 注入待确认习惯（温和提醒，不再硬性阻断）
    ST->>ST: 注入计划摘要
    ST-->>OC: 返回 system prompt

    OC->>LLM: 发送完整上下文

    Note over OC,CM: 用户消息通过 hook
    OC->>CM: 触发
    CM->>CM: 捕获用户消息文本
    CM->>CM: 检测"确认习惯"关键词
    CM->>CM: 注入 pendingWarnings（触发线2/4）
    CM-->>OC: 返回消息

    LLM-->>OC: 生成回复（含工具调用）

    Note over OC,EV: 每个事件触发 hook
    OC->>EV: message.updated
    EV->>EV: 检测 websearch 调用
    EV->>EV: 检测断言（触发线4）
    EV->>EV: 检测 file.edited → 入队审查
    EV->>EV: 检测 session.idle → 消费队列 + 习惯提醒

    OC-->>U: 显示回复
```

---

## 四、触发线 1：文件编辑审查

**场景**：你写了一个文件，分形检查它是否匹配某个已注册的习惯规则。

**核心改进（V3.6）**：不再在 `file.edited` 事件发生时立即注入审查——改为**队列化 + 等待 session 结束**，避免在 agent 回复过程中打断。

```mermaid
sequenceDiagram
    participant A as Agent
    participant EV as event hook
    participant Q as pendingReviewQueue
    participant LLM as LLM API
    participant S as Session

    Note over A,S: Agent 正在回复用户（可能多次编辑文件）

    A->>EV: file.edited("分形/fractal.ts")
    EV->>EV: glob 预筛选（匹配 trigger 规则）
    EV->>Q: push({ filePath, trigger, sessionID })
    Note over EV: 不做 LLM 调用，不注入消息

    A->>EV: file.edited("分形/设计.md")
    EV->>Q: push({ filePath, trigger, sessionID })

    Note over A,S: Agent 完成回复

    A->>EV: session.idle
    EV->>Q: flushReviewQueue()
    Q-->>EV: 返回 2 条待审查项

    loop 每条审查
        EV->>LLM: generateTriggerMessage(filePath, trigger)
        LLM-->>EV: 赛博分身消息 或 空（不匹配）
        EV->>S: promptAsync({ noReply: true, text: 消息 })
        Note over S: 注入上下文，不触发 Agent 回复
    end
```

**三层漏斗**（仅第二、三层改为 session.idle 时执行）：

| 层 | 执行时机 | 做什么 | 耗时 | Token |
|----|---------|--------|------|-------|
| 1. glob 预筛选 | `file.edited` 事件时 | 检查文件路径是否匹配 trigger 的 glob 规则 | <1ms | 0 |
| 2. LLM 语义判断 | `session.idle` 时 | 调 LLM API 判断文件改动是否真正匹配习惯语义 | ~3s | ~200 |
| 3. 消息注入 | LLM 返回后 | `promptAsync({ noReply: true })` 注入赛博分身消息 | ~100ms | 0 |

**队列上限**：20 条。超过上限时丢弃最旧项，防止 session.idle 不触发时无限增长。

---

## 五、触发线 2：无进展循环 + 无反馈环

**场景**：Agent 连续修改代码但不执行测试，陷入"改→不行→再改→还不行"的死循环。

```mermaid
flowchart TD
    A[新一轮消息到达<br/>message.updated: user] --> B{上一轮有编辑？}
    B -->|否| NOP[保持计数不变]
    B -->|是| C{上一轮有 bash 调用？}
    C -->|是| RST[重置 consecutiveTurns = 0<br/>反馈环存在]
    C -->|否| INC[consecutiveTurns++<br/>无反馈环]
    INC --> D{turns ≥ 阈值3？}
    D -->|否| NOP
    D -->|是| INJ["system.transform 注入警告<br/>重置计数"]
```

**检测时机**：每轮 `message.updated`（user 角色消息）时检查。

**注入方式**：下一轮 `system.transform` 中注入纯模板警告（不调 LLM）。

---

## 六、触发线 4：主动联网查证（分级升级）

**场景**：Agent 说"XX 不支持"但没有联网查证——你训过它别猜，但它还是猜了。

```mermaid
sequenceDiagram
    participant A as Agent
    participant EV as event hook
    participant CT as 计数器（文件持久）
    participant ST as system.transform
    participant U as 用户

    Note over A,U: 第 1 轮：首次断言未查证

    A->>A: "这个 API 不支持批量操作"（断言）
    EV->>EV: 正则匹配断言 ✅
    EV->>EV: 检查本轮 websearch 调用 ❌ 无
    EV->>CT: count = 1

    Note over A,U: 第 2 轮：system.transform 注入提醒

    ST->>CT: 读取 count
    CT-->>ST: count = 1
    ST->>ST: 注入等级 1：温和提醒

    Note over A,U: Agent 又犯了一次

    A->>A: "OC 只有 3 个 hook"（断言）
    EV->>CT: count = 2
    ST->>ST: 注入等级 2：强硬提醒

    Note over A,U: Agent 查证了

    A->>A: websearch("OC hooks 完整列表")
    EV->>EV: 检测到 websearch ✅
    EV->>CT: count = 0（正确行为重置）

    Note over A,U: 连续 3 轮无断言

    ST->>CT: 衰减 count
    CT-->>ST: count = 0
```

**分级表**：

| 等级 | count | 注入内容 | 语气 |
|------|-------|---------|------|
| 0 | 0~0 | 不注入 | — |
| 1 | 1 | "上轮你说了'不支持'但没有联网查证——先查再下结论？" | 温和 |
| 2 | 2~3 | "连续 N 次断言未查证！涉及未知 API 时先调 websearch。" | 强硬 |
| 3 | ≥4 | "本轮回复前如涉及能力判断必须先调 websearch。" | 强制 |

**计数器管理**：
- 断言 + 无查证 → `count++`
- 断言 + 有查证 → `count = 0`（重置）
- 连续 3 轮无断言 → `count = max(0, count - 1)`（衰减）
- 会话切换（sessionID 变化） → `count = 0`

---

## 七、触发线 5：提交后知识提取

**场景**：你刚提交了一个重要修复，但没记下来——下次遇到同类问题还得重新研究。

```mermaid
sequenceDiagram
    participant ST as system.transform
    participant Git as Git
    participant LLM as LLM API
    participant BLK as blocks/

    Note over ST,BLK: 每轮 system.transform 执行

    ST->>Git: git log -1 --format="%H %aI"
    Git-->>ST: 最新 commit hash + 时间戳

    ST->>ST: 对比 .commit-last-check.json
    alt 时间戳相同（无新提交）
        ST->>ST: 跳过
    else 有新提交
        ST->>Git: git log -1（完整信息 + stat）
        Git-->>ST: commit message + 文件列表
        ST->>ST: 过滤：跳过 Merge/Bump/chore
        ST->>LLM: 分析 commit message + diff<br/>判断是否值得记录
        LLM-->>ST: 有价值 → 生成知识块
        ST->>BLK: 写入 blocks/pending/xxx.md
        ST->>ST: 更新 .commit-last-check.json
    end
```

**为什么用轮询而非事件 hook？** OC 1.18.3 实测不触发 `tool.execute.before/after` 事件。`system.transform` 每轮都执行，轮询 `git log` 时间戳是唯一可靠的检测方式。

---

## 八、习惯确认：完整流程

```mermaid
sequenceDiagram
    participant S as session.idle
    participant EV as event hook
    participant U as 用户
    participant CM as chat.message
    participant Q as question 工具
    participant BLK as blocks/

    Note over S,BLK: V3.6：从硬性阻断改为温和提醒

    S->>EV: session 结束
    EV->>EV: injectPendingHabitReminder()
    EV->>BLK: getBlocksCached() 读取 pending 项
    BLK-->>EV: 2 条待确认习惯

    EV->>S: promptAsync({ noReply: true })
    Note over S: "🟡 有待确认的习惯<br/>说'确认习惯'即可确认"

    Note over U: ...用户继续工作，几轮后有空了...

    U->>CM: "确认习惯"
    CM->>CM: 正则匹配 ✅
    CM->>BLK: getBlocksCached() 读取 pending 项
    CM->>CM: push 确认指令到 pendingWarnings
    CM->>CM: unshift 到用户消息前面

    Note over Q: Agent 看到指令

    Q->>U: question("分形发现了习惯「…」，是否保存？")
    U->>Q: 选择"自动·全局"

    Note over BLK: Agent 编辑文件
    BLK->>BLK: status: pending → auto<br/>移至 blocks/auto/
```

**关键变化（V3.5 → V3.6）**：

| | V3.5 | V3.6 |
|---|------|------|
| 触发时机 | 每轮 system.transform（打断） | session.idle（温和） |
| 注入方式 | output.system.push（硬性规则） | promptAsync({ noReply: true })（上下文消息） |
| 用户交互 | 强制立即确认 | 说"确认习惯"时触发 |
| core-rules.md | 含硬性阻断规则 | 删除 |

---

## 九、记忆系统：三层存储 + 自动学习

### 存储结构

```
memories/
├── blocks/                   ← 知识块（按 status 分目录）
│   ├── pending/              ← 待用户确认
│   │   └── edit-then-review.md
│   ├── auto/                 ← 已确认，Agent 自动执行
│   │   └── scss-format-on-save.md
│   └── suggest/              ← 观察中，Agent 参考
│       └── prefer-fetch-over-curl.md
├── triggers/                 ← 触发规则（按 status 分目录）
│   ├── pending/
│   ├── auto/
│   └── suggest/
├── events.log                ← 事件原始日志
├── debug.log                 ← 诊断日志
├── .assertion-counter.json   ← 触发线4 分级计数
└── .commit-last-check.json   ← 触发线5 上次提交时间戳
```

### LLM 自主学习分析（完整流程）

这是分形最复杂的子系统——从海量事件中发现你的行为模式，自动创建知识块和触发规则。

```mermaid
sequenceDiagram
    participant ST as system.transform
    participant EVT as events.log
    participant PROMPT as LLM Prompt
    participant LLM as LLM API
    participant PARSE as 响应解析
    participant DEDUP as 除重检查
    participant BLK as blocks/
    participant TRG as triggers/

    Note over ST,TRG: 每轮 system.transform 执行

    ST->>EVT: getNewEvents() 读取增量
    EVT-->>ST: N 条新事件

    alt N < 动态阈值 AND 非强制学习
        ST->>ST: 跳过
    else N ≥ 阈值 OR /fractal learn
        Note over PROMPT: 构造分析 prompt

        ST->>PROMPT: 加载已有习惯列表（作为反例）
        ST->>PROMPT: 加载事件序列（最近 N 条）
        ST->>PROMPT: 加载分析规则（判断标准 + 去重约束）

        ST->>LLM: POST /chat/completions<br/>{ thinking: disabled, temperature: 0.3 }
        LLM-->>ST: JSON 响应

        ST->>PARSE: 解析 type 字段
        alt type = "skip"
            PARSE->>ST: 无新发现（或语义重复已有习惯）
        else type = "create" 或 "update"
            ST->>DEDUP: 检查是否与已有习惯语义重复
            DEDUP-->>ST: 通过 / 跳过
            alt 通过除重
                ST->>BLK: writeBlock({ description, content, status: pending })
                ST->>TRG: writeTrigger({ glob, humanDescription, confidence })
                Note over BLK,TRG: 写入 blocks/pending/ 和 triggers/pending/<br/>元数据含 suggested_status + memPathIndex
            end
        end
    end
```

#### 事件收集

`event hook` 把所有系统事件以 JSON 行格式追加到 `events.log`：

```json
{"ts":"2026-07-23T10:30:01Z","type":"message.updated","properties":{...}}
{"ts":"2026-07-23T10:30:02Z","type":"file.edited","properties":{"file":"src/app.ts"}}
{"ts":"2026-07-23T10:30:05Z","type":"tool.execute.after","properties":{...}}
```

每轮 `system.transform` 调用 `getNewEvents()` 读取上次分析时间戳之后的所有新事件。

#### 动态阈值

| 分析次数 | 阈值（事件数） |
|---------|--------------|
| 第 1 次 | ≥ 20 |
| 第 2 次 | ≥ 40 |
| 第 3 次 | ≥ 80 |
| 第 4 次 | ≥ 160 |
| 第 5+ 次 | ≥ 400（上限） |

设计意图：前几次频繁分析以快速建立基线，之后指数退避避免过度分析。上限 400 保证即使在长会话中也不会完全沉默。

**强制触发**：输入 `/fractal learn` 会在下一轮 system.transform 中写入 `LEARN_FLAG`，绕过阈值直接触发分析。

#### LLM Prompt 构造

分析 prompt 包含三个部分（定义在 `lib/prompts.ts`）：

| 部分 | 内容 | 用途 |
|------|------|------|
| **系统提示** | 分析规则、输出格式、反例约束 | 定义 LLM 角色为"行为模式分析师" |
| **已有习惯** | 所有 auto/suggest 习惯的 `human_description` 列表 | 作为反例，要求 LLM 检测到语义相似时输出 `skip` |
| **事件序列** | 最近 N 条事件的 summary（类型 + 关键字段） | 供 LLM 分析的原始数据 |

**反例约束（创建层去重）**：
```
6. 新习惯与已有习惯语义相似 → 必须返回 type="skip"
   判断标准：如果新发现的模式只是已有习惯的另一种说法，
   或者两者覆盖的触发场景高度重叠 → 跳过。
```

#### LLM 响应解析

LLM 返回 JSON，包含 `type` 字段：

| type | 含义 | 后续动作 |
|------|------|---------|
| `"skip"` | 无新习惯 / 语义重复 | 仅更新时间戳 |
| `"create"` | 发现新模式 | 创建 block + trigger（status=pending） |
| `"update"` | 增强已有习惯 | 更新现有 block 的 content / trigger 的 glob |

每条 create/update 还包含：
- `description`：人类可读的一句话描述
- `content`：详细说明（事实 → 原则 → 反例 → 结论）
- `human_description`：trigger 的简短标签
- `match_globs`：逗号分隔的 glob 模式（如 `*.ts,*.vue`）
- `confidence`：高 / 中 / 低
- `suggested_status`：建议确认后的 status（suggest / auto）
- `memPathIndex`：建议的存储层级（0=全局 / 1=个人项目 / 2=共享项目）

#### 文件元数据

创建的 block 文件头部包含元数据行：

```markdown
<!-- type: knowledge --><!-- status: pending --><!-- description: 触发线4断言正则的盲区 -->
<!-- priority: 45 --><!-- suggested_status: auto --><!-- memPathIndex: 2 -->

## 触发线 4 ASSERTION_RE 的盲区
...
```

Trigger 文件结构类似，额外包含 `match_globs` 和 `confidence` 字段。

#### 双路除重机制

多条路径防止同一习惯被重复创建：

```mermaid
flowchart TD
    subgraph 路1["路 1：注入层除重（治标）"]
        A1["system.transform 读取所有 pending"] --> A2["extractKeywords() 两两比较"]
        A2 --> A3{"关键词重叠率 ≥ 50%？"}
        A3 -->|是| A4["只保留优先级最高的一条"]
        A3 -->|否| A5["全部注入"]
    end

    subgraph 路2["路 2：创建层除重（治本）"]
        B1["LLM 分析 prompt 加载已有习惯列表"] --> B2{"新习惯与已有习惯<br/>语义相似？"}
        B2 -->|是| B3["输出 type: skip"]
        B2 -->|否| B4["输出 type: create"]
    end

    A1 -.->|"不同轮次可能<br/>创建同名习惯"| B1
```

**为什么不是 100% 去重？** 语义相似 ≠ 同一习惯。宁可漏掉让用户手动确认，也不合并两个独立习惯。

#### 从 pending 到 auto：习惯的生命周期

```mermaid
stateDiagram-v2
    [*] --> pending: LLM 自主学习发现<br/>写入 blocks/pending/

    state pending {
        [*] --> 等待
        等待 --> 提醒: session.idle 温和提醒
        提醒 --> 等待: 用户忽略
    }

    pending --> auto: 用户确认<br/>选"自动·全局/本项目"
    pending --> suggest: 用户确认<br/>选"建议·全局/本项目"
    pending --> [*]: 用户选"不保存"<br/>删除文件

    auto --> [*]: PageOut 衰减<br/>（长期未命中）
    suggest --> [*]: PageOut 衰减
```

PageOut 衰减机制：知识块在注入时会根据命中率加分/衰减。长期未被引用的 `auto` 知识块权重持续降低，最终退出注入集会，但仍保留在磁盘中。

---

## 十、注入频率控制

并非所有内容每轮都注入——分级控制减少 system prompt 膨胀：

| 内容 | 频率 | 理由 |
|------|------|------|
| 核心规则（core-rules.md） | 每轮 | Agent 身份/行为底线 |
| 断言提醒（触发线4） | 每轮（当 count > 0） | 紧急，需立即纠正 |
| 联网查证规则 | 每轮 | 预防性规则 |
| 知识索引（knowledge） | 每 5 轮 | 量大，可节流 |
| 已确认习惯（auto） | 每 5 轮 | 已内化，不需要高频 |
| 观察中习惯（suggest） | 每 5 轮 | 不确定，低频提示 |
| 计划摘要 | 每轮 | 导航信息，轻量 |
| 无反馈环警告（触发线2） | 触发时 1 次 | 告警后重置 |

**关键词命中例外**：即使用户消息命中了 knowledge 关键词，非 nudge 轮也会立即注入匹配的 knowledge 项。

---

## 十一、数据流全景

```mermaid
flowchart LR
    subgraph 输入
        FE[file.edited]
        MU[message.updated]
        CM[chat.message]
        SI[session.idle]
        SS[session.status]
        GC[新 git commit]
    end

    subgraph 处理
        T1[触发线1<br/>审查队列]
        T2[触发线2<br/>滑动窗口]
        T4[触发线4<br/>断言计数]
        T5[触发线5<br/>知识提取]
        AL[LLM 自主学习]
    end

    subgraph 输出
        SIJ[system prompt 注入]
        CIJ[chat.message 注入]
        PIJ[promptAsync 注入]
        BLK[blocks/ 写入]
        TRG[triggers/ 写入]
    end

    FE --> T1
    SI --> T1
    SS --> T1
    MU --> T2
    MU --> T4
    CM --> T2
    CM --> T4
    GC --> T5
    MU --> AL

    T1 --> PIJ
    T2 --> SIJ
    T2 --> CIJ
    T4 --> SIJ
    T5 --> BLK
    AL --> BLK
    AL --> TRG
```

---

## 十二、关键常量速查

| 常量 | 值 | 用途 |
|------|-----|------|
| `NUDGE_INTERVAL` | 5 | knowledge/habits 注入间隔（轮） |
| `ANALYSIS_THRESHOLD` | 20 | 首次 LLM 自主学习阈值（事件数） |
| `ANALYSIS_THRESHOLD_MAX` | 400 | 自主学习阈值上限 |
| `NO_FEEDBACK_THRESHOLD` | 3 | 无反馈环告警阈值（连续 edit 无 bash） |
| `PENDING_QUEUE_MAX` | 20 | 触发线1 审查队列上限 |
| `BLOCKS_CACHE_TTL` | 5000ms | mergeBlocksAndTriggers 缓存有效期 |
| `ASSERTION_THRESHOLDS` | 1,3,5 | 触发线4 分级阈值（可自定义） |

---

> 技术细节与源码实现见 [设计.md](设计.md) 和 `fractal.ts`。
